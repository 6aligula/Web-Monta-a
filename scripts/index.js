// index.js
import { cargarHeader, cargarFooter } from './common.js';

// Cargar el header y footer al cargar la página
document.addEventListener('DOMContentLoaded', () => {
    cargarHeader();
    cargarFooter();
    // Cualquier otro código específico para index.html
});
